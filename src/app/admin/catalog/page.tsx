import { CatalogPage } from "@/components/pages/admin/catalog-page/CatalogPage";
import React from "react";

interface Props {}

const page = (props: Props) => {
  return (
    <div>
      <CatalogPage />
    </div>
  );
};

export default page;
